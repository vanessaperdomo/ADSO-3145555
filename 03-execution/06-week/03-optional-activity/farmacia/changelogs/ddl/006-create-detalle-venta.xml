<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!--
        Tabla: detalle_venta
        Líneas de cada venta: qué medicamentos se vendieron y en qué cantidad.
    -->
    <changeSet id="ddl-006-create-detalle-venta" author="laura">
        <comment>Crea la tabla de líneas de detalle de cada venta</comment>

        <createTable tableName="detalle_venta" remarks="Ítems individuales de cada venta">
            <column name="id" type="SERIAL">
                <constraints primaryKey="true" primaryKeyName="pk_detalle_venta" nullable="false"/>
            </column>
            <column name="venta_id" type="INT" remarks="FK a la venta padre">
                <constraints nullable="false"
                             foreignKeyName="fk_detalle_venta"
                             referencedTableName="venta"
                             referencedColumnNames="id"/>
            </column>
            <column name="medicamento_id" type="INT" remarks="FK al medicamento vendido">
                <constraints nullable="false"
                             foreignKeyName="fk_detalle_medicamento"
                             referencedTableName="medicamento"
                             referencedColumnNames="id"/>
            </column>
            <column name="cantidad" type="INT" remarks="Unidades vendidas de este medicamento">
                <constraints nullable="false"/>
            </column>
            <column name="precio_unitario" type="NUMERIC(12,2)" remarks="Precio de venta en el momento de la transacción">
                <constraints nullable="false"/>
            </column>
            <column name="subtotal" type="NUMERIC(14,2)" remarks="cantidad × precio_unitario">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <sql>
            ALTER TABLE detalle_venta
            ADD CONSTRAINT chk_detalle_cantidad
                CHECK (cantidad > 0);

            ALTER TABLE detalle_venta
            ADD CONSTRAINT chk_detalle_precio
                CHECK (precio_unitario > 0);

            ALTER TABLE detalle_venta
            ADD CONSTRAINT chk_detalle_subtotal
                CHECK (subtotal > 0);
        </sql>

        <rollback>
            <dropTable tableName="detalle_venta"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
